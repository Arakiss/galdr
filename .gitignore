/target
**/*.rs.bk
Cargo.lock.bak

# local state (galdr keeps everything under ~/.galdr, never in the repo)
*.sqlite
*.sqlite-wal
*.sqlite-shm

# local-only HTML artifacts (status hubs, audits, reports — never published)
/html-artifacts/

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp
